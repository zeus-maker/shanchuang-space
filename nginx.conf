server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Handle /huobao-canvas path
    location /huobao-canvas {
        alias /usr/share/nginx/html/huobao-canvas;
        try_files $uri $uri/ /huobao-canvas/index.html;
    }

    # API proxy to backend
    location /v1 {
        proxy_pass https://api.chatfire.site;
        proxy_set_header Host api.chatfire.site;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_server_name on;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
