# README 启动说明整理

**日期**: 2026-04-02

## 背景与原因

本地媒体服务与 `dev:all` 加入后，单一「npm run dev」说明不足以覆盖推荐启动方式、访问路径（`/huobao-canvas`）、生产 `start` 与 Docker。

## 修改点

- 扩充 **README「快速开始」**：安装、`.env` 提示、开发三种启动方式对照表、构建与 `npm run start`、环境变量表、可选 Docker 命令。
- **「本地媒体缓存」** 小节改为摘要并指回快速开始与 `nginx.conf`，减少重复。

## 复盘

截图仍引用 `./doc/` 下资源；若仓库已迁至 `docs/doc/`，可另起任务统一替换链接。

**涉及模块**：`README.md`
