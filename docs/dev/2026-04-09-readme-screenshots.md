# README 完善与截图说明对齐

## 背景与原因

根目录 `README.md` 中「截图」一节仅有占位说明，未嵌入实际资源；仓库已在 `docs/images/项目截图/` 下提供四张产品界面图，需在 README 中以 GitHub 常见写法（相对路径、简要说明）展示，并借机补充与代码一致的节点类型、边类型、路由、编排类型与目录结构，降低新贡献者上手成本。

## 修改点

- 重写 `README.md`：增加目录锚点、功能表格、四张截图及中文说明、节点与边注册表、智能编排工作流类型表、路由表、仓库树状结构摘要，并修正原「请将图片放在 docs/doc」等过时提示。
- 截图路径统一为 `./docs/images/项目截图/*.png`，与仓库内文件一致。

## 复盘

说明性文档应与 `Canvas.vue` 中 `nodeTypes` / `edgeTypes`、`router/index.js` 的 `base` 保持一致；后续若新增节点类型，宜同步更新 README 表格或改为指向 `docs/TECH.md` 的单一事实来源以免重复漂移。
