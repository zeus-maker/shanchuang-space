---
description: 修复/交付留档（docs/dev + git）与向用户回复的格式与质量（简体中文）
alwaysApply: true
---

# 交付与留档（简体中文）

## A. 向用户回复的格式与质量

在完成 **Bug 修复、功能实现、配置调整、排查结论** 等任务后，最终对用户的说明应 **结构清晰、可直接执行**，避免碎碎念与空泛套话。

### 推荐结构（按任务选用，不必强行凑段）

1. **原因说明**（适用修复/行为变更时）  
   - 简述**现象**与**根因**（或设计取舍），必要时一句话带过触发条件。  
   - 不写冗长堆栈；密钥、token、隐私数据 **禁止** 出现在回复中。

2. **已做调整**（必有）  
   - 用列表说明 **改了什么**（模块/文件级即可，不必逐行）。  
   - 用户**能感知到的行为变化**写清楚。

3. **你需要做的**（若有）  
   - 环境动作：重启 dev、复制 `.env`、清缓存等。  
   - 配置项名写 **完整**（如 `VITE_VOLCENGINE_BASE_URL`）。

4. **验证方式**（可选）  
   - 一两步可复现的确认方式（点哪里、预期现象）。

5. **记录与提交**（若本次已执行）  
   - 一句话：`docs/dev/xxx.md`、commit 哈希或类型（不必贴大段 diff）。

### 质量要求

- **语言：** 简体中文；句子完整，少用电报体。  
- **篇幅：** 与任务复杂度匹配；小改短答，大改再分层级标题。  
- **诚实：** 未测/未跑 CI 则说明；假设与风险可简短标注。  
- **安全：** 不复述用户提供的密钥；提醒敏感信息勿入库时可单独一句。  
- **代码引用：** 若需指代码，用仓库要求的行号引用格式，避免整文件粘贴。

---

## B. 问题修复：`docs/dev` 记录与 Git 提交

完成 **Bug 修复、行为修正、线上/本地问题排查** 等「修复类」工作后，按顺序完成以下事项（用户明确声明不要提交时除外）。

**说明：** 若本次为 **功能迭代** 且用户或团队希望留档，同样可在 `docs/dev/` 新增记录，结构与修复类一致（背景、修改点、复盘）。

### 1. 撰写 `docs/dev/` 记录

在 **`docs/dev/`** 新增 Markdown（同一次修复可追加同一文件）。

- **命名：** `YYYY-MM-DD-<简短英文 slug>.md`
- **结构：** 背景与原因、修改点、复盘（简体中文，勿写入真实 Key/token）

### 2. 提交 Git

在仓库根目录执行：

```bash
git status
git add <涉及文件>
git commit
```

**提交说明（commit message）须详细**，推荐结构：

```
<类型>(<范围>): <一句话摘要>

- 原因：<现象 / 根因>
- 修改：<主要文件与行为变化，可列要点>
- 记录：docs/dev/<对应文件名>.md
```

- **类型：** 常用 `fix`；文档/规则-only 可用 `docs` 或 `chore`；功能交付可用 `feat`
- **范围：** 可选，如 `image-api`、`canvas`；无则省略括号
- **摘要：** 简体中文，说明「做了什么」，避免泛泛的「修复 bug」

示例：

```
fix(image): Seedream 生图改走火山引擎 Ark

- 原因：全局渠道为 DeepSeek 时，Seedream 仍请求 DeepSeek 端点导致失败。
- 修改：useImageGeneration 按模型选 volcengine；providers 适配请求/响应；请求头与 ImageConfig 校验逻辑调整。
- 记录：docs/dev/2025-03-26-seedream-routes-to-volcengine.md
```

若一次 commit 含多条独立修复，摘要中概括主题，正文分条列出各条原因与文件。

### 与代码变更的关系

`docs/dev` 记录与对应代码变更应在 **同一 commit**（或紧密连续的 logically-one-fix 提交）中完成，便于对照与回滚。

### 与上一节的分工

- **聊天回复：** 给用户看的摘要与操作指引（本节 **A**）。  
- **`docs/dev`：** 团队留档（本节 **B**）。
