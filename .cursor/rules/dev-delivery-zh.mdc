---
description: 交付与留档：用户回复、docs/dev、Git 提交共用同一信息骨架（简体中文）
alwaysApply: true
---

# 交付与留档（简体中文）

## 适用范围

完成 **Bug 修复、功能实现、配置调整、排查结论** 等工作后，同时满足：

- **对用户**：最终回复结构清晰、可执行。  
- **对仓库**（用户未声明豁免时）：`docs/dev` 留档 + 带说明的 `git commit`。

豁免：用户明确说 **不要提交** 或 **不要写 docs/dev** 时，仅做聊天说明即可。

---

## 统一信息骨架（先按此想清楚，再分别写入聊天 / 文档 / commit）

四块内容 **对齐一致**，详略可以不同（聊天略写、docs/dev 可更全）：

| 骨架 | 含义 | 聊天回复里 | `docs/dev` 里 | `commit message` 里 |
|------|------|------------|---------------|---------------------|
| **原因** | 现象、根因或设计取舍 | § 原因说明 | **背景与原因** | `- 原因：…` |
| **修改** | 改了什么、用户可感知行为 | § 已做调整（列表） | **修改点**（行为与设计为主，少罗列函数/字段；代码路径仅作索引） | `- 修改：…` |
| **验证** | 如何确认（可选） | § 验证方式（可选） | 可写入 **复盘** 或省略 | 一般省略 |
| **记录** | 留档文件名与提交标识 | § 记录与提交（一句） | 文件即本体 | `- 记录：docs/dev/xxx.md` |

密钥、token、隐私 **禁止** 出现在聊天、`docs/dev` 与 commit 中。

---

## 对用户回复（聊天）

按骨架组织段落（不必每段都写满，**已做调整** 必有）：

1. **原因说明**（修复/行为变更时）：现象 + 根因或取舍，一句话点触发条件即可；不写长堆栈。  
2. **已做调整**：列表写模块/文件级变更 + **用户能感知的行为**。  
3. **你需要做的**（若有）：环境动作；配置项写全名（如 `VITE_VOLCENGINE_BASE_URL`）。  
4. **验证方式**（可选）：一两步操作与预期。  
5. **记录与提交**（若已执行）：一句话写 `docs/dev/xxx.md` 与 commit 类型/哈希，不贴大段 diff。

**质量：** 简体中文、完整句、篇幅与任务匹配；未测则说明；不复述用户密钥；指代码用仓库要求的行号引用格式。

---

## 对仓库：`docs/dev` 与 Git

与上表 **同一套原因 / 修改**，避免聊天与文档各说一套话。

### `docs/dev/` 文件

- **命名：** `YYYY-MM-DD-<简短英文 slug>.md`  
- **结构：** 背景与原因、修改点、复盘（简体中文）  
- **功能迭代** 若需留档，结构与修复类相同。

#### 文档写法（质量）

- **优先写「为什么」与「用户/系统表现」**：需求或问题来源、设计取舍、边界（刻意不做什么）、与旧行为差异、兼容策略。  
- **少写实现清单**：避免把留档写成「函数名 / 字段名 / 调用链」堆砌；代码细节以 Git 与源文件为准。  
- **修改点** 宜描述**可感知行为与模块级范围**（例如「画布分组随项目保存、支持撤销」）；若需对照代码，文末可加一句 **「涉及模块」** 列出主要路径即可。  
- **修复类** 可保留现象与根因；**功能类** 侧重需求对齐与产品设计说明，复盘里可记文档教训或未完项。

### `git commit`

```bash
git status
git add <涉及文件>
git commit
```

**message** 推荐：

```
<类型>(<范围>): <一句话摘要>

- 原因：<与 docs 背景一致，可压缩>
- 修改：<与 docs 修改点一致，可压缩>
- 记录：docs/dev/<对应文件名>.md
```

- **类型：** `fix` 常见；纯文档/规则 `docs` 或 `chore`；功能交付 `feat`  
- **范围：** 可选，如 `canvas`、`image-api`

若一次 commit 含多条独立变更：摘要概括主题，正文分条写原因与文件。

**与代码关系：** `docs/dev` 与对应代码宜在 **同一 commit**（或紧密连续、逻辑上同一交付），便于对照与回滚。

---

## 复盘

助手在收尾时自检：**聊天里的「原因 / 修改 / 记录」是否与 `docs/dev` 和 commit 三处同源**，避免只回用户不写档或文档与提交脱节。
