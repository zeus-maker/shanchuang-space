---
description: 项目架构总览与通用编码约定（画布节点应用）
alwaysApply: true
---

# 项目架构与编码约定

## 技术栈

| 层 | 技术 |
|----|------|
| 框架 | Vue 3 Composition API + Vite |
| 画布 | Vue Flow（`@vue-flow/core`） |
| 状态 | Pinia（`src/stores/pinia/`）+ 手动 reactive store（`src/stores/canvas.js`、`src/stores/projects.js`） |
| UI | Naive UI（按需导入） |
| 图标 | `@vicons/ionicons5`（Outline 后缀） |
| 样式 | Tailwind CSS（utility class） + CSS 变量主题 |
| API | `src/api/`（`chat.js` / `image.js` / `video.js` / `model.js`） |

---

## 目录结构要点

```
src/
  api/               # 接口封装（streamChatCompletions, generateImage…）
  components/
    nodes/           # 节点组件（每种类型一个 .vue 文件）
    edges/           # 自定义边类型
  config/
    models.js        # 模型常量（DEFAULT_CHAT_MODEL / DEFAULT_IMAGE_MODEL…）
    providers.js     # 供应商配置
  hooks/             # 组合式函数（useChat、useWorkflowOrchestrator…）
  stores/
    canvas.js        # 画布核心状态（nodes / edges / canvasGroups）
    projects.js      # 项目列表与持久化
    pinia/           # Pinia stores（models、providers…）
  views/
    Canvas.vue       # 主画布页（Vue Flow 宿主、工具栏、打组 UI）
```

---

## Canvas 状态（canvas.js）

- **只通过导出函数修改状态**，不直接赋值 `nodes.value`（除批量操作）。
- 常用导出：`updateNode(id, patch)` / `addNode(type, pos, data?)` / `addNodes(specs[])` / `addEdge(spec)` / `removeNode(id)` / `duplicateNode(id)`
- 批量操作配对使用：`startBatchOperation()` … `endBatchOperation()`
- 打组：`addCanvasGroup(ids[]) → groupId`（至少 2 个成员才创建，返回 id 或 null）；`updateCanvasGroup(id, partial)` 可更新任意字段（含 `label`）；`removeCanvasGroup(id)`

---

## Vue Flow 使用规范

- 节点组件**动态改变尺寸后**（如状态切换导致高度/宽度变化），必须调用 `updateNodeInternals(props.id)` 通知 Vue Flow 重算边的连接点位置，通常写在：
  ```js
  watch(变化的响应式值, () => nextTick(() => updateNodeInternals(props.id)))
  ```
- 打组框 UI 通过 Vue Flow 的 **`#zoom-pane` 插槽**渲染，使其与节点同处同一坐标变换层。
- Handle 放在节点卡片内部，`id` 用语义名（`left` / `right` 或 `script-input`），连接点默认居中。

---

## 节点组件约定

- `defineProps({ id: String, data: Object })`，**只通过 `updateNode(props.id, patch)` 写回状态**，不直接改 `props.data`。
- 每个节点需在以下三处注册（见「新增节点类型」规则文件）：
  1. `src/components/nodes/XxxNode.vue`
  2. `src/stores/canvas.js` → `estimateNodeSize` + `getDefaultNodeData`
  3. `src/views/Canvas.vue` → `nodeTypes` + `tools`

---

## 主题 CSS 变量（深色优先）

| 变量 | 用途 |
|------|------|
| `--bg-primary` | 页面底色 |
| `--bg-secondary` | 卡片底色 |
| `--bg-tertiary` | 输入框/hover 背景 |
| `--text-primary` | 主文字 |
| `--text-secondary` | 次要文字 |
| `--text-tertiary` | 占位符/标注 |
| `--border-color` | 通用边框 |
| `--accent-color` | 品牌强调色 |

节点卡片背景统一用 `bg-[var(--bg-secondary)]`，边框用 `border-[var(--border-color)]`。

---

## 禁止事项

- **不要**在节点组件内直接操作 `nodes.value` 或 `edges.value`，统一走 canvas store 函数。
- **不要**把 `streamChatCompletions` 的 signal 遗漏：调用前创建 `AbortController`，`onUnmounted` 时 `abort()`。
- **不要**在打组 DOM（`#zoom-pane` 插槽内容）上设置 `pointer-events: auto` 的覆盖层，会遮挡节点交互。
